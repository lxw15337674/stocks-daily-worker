{
  "name": "china-stocks-daily-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "dev": "pnpm -r --parallel --stream --filter @china-stocks/api --filter @china-stocks/web run dev",
    "cf:login": "wrangler login",
    "cf:whoami": "wrangler whoami",
    "cf:d1:create:stocks": "wrangler d1 create china-stocks-daily --config apps/api/wrangler.toml",
    "cf:d1:create:crypto": "wrangler d1 create crypto-daily --config apps/api/wrangler.toml",
    "db:sync:local": "node --experimental-strip-types scripts/sync-d1-to-local.ts",
    "db:sync:local:stocks": "node --experimental-strip-types scripts/sync-d1-to-local.ts stocks --yes",
    "db:sync:local:crypto": "node --experimental-strip-types scripts/sync-d1-to-local.ts crypto --yes",
    "dev:api": "wrangler dev --local --config apps/api/wrangler.toml",
    "dev:web": "node --experimental-strip-types scripts/select-web-config.ts local && pnpm --dir apps/web run dev",
    "build": "pnpm --dir apps/web run build",
    "check": "pnpm run check:api && pnpm run check:web",
    "test": "bun test tests/contracts.market.test.ts tests/api.indices.test.ts tests/api.scheduler-status.test.ts tests/api.routes.test.ts tests/web.market-loaders.test.ts tests/web.status-loaders.test.ts tests/web.client-fetch.test.ts tests/web.client-hooks.test.tsx",
    "check:api": "pnpm --dir apps/api run check",
    "check:web": "node --experimental-strip-types scripts/select-web-config.ts remote && pnpm --dir apps/web run check",
    "deploy:api": "wrangler deploy --config apps/api/wrangler.toml",
    "deploy:web": "node --experimental-strip-types scripts/select-web-config.ts remote && pnpm --dir apps/web run deploy",
    "db:generate": "pnpm --dir apps/api run db:generate",
    "db:studio": "pnpm --dir apps/api run db:studio"
  },
  "devDependencies": {
    "@mozilla/readability": "0.6.0",
    "linkedom": "0.18.12",
    "playwright": "1.53.2",
    "wrangler": "^4.73.0"
  }
}
